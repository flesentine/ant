'use strict';const assert=require('assert');const{Simulation}=require('../src/sim-core.js');const{loadBundle}=require('../tools/load-bundle.js');const sim=new Simulation(loadBundle('open_arena_short_control.json'),1);assert.strictEqual(sim.model.id,'lasius_niger_locomotion_v1');assert.strictEqual(sim.apparatus.id,'poissonnier2026_open_arena');assert.strictEqual(sim.experiment.protocol.approach_distance_mm,200);assert(!('movement' in sim.experiment));console.log('experiment-definition.test.js PASS');
