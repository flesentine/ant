'use strict';const assert=require('assert'),path=require('path');const{readJson}=require('../tools/load-bundle.js');const{assertDatasetAllowed}=require('../tools/calibration-policy.js');const m=readJson(path.resolve(__dirname,'../reference/calibration_manifest.json'));assert.doesNotThrow(()=>assertDatasetAllowed(m,'poissonnier2026_open_arena'));assert.throws(()=>assertDatasetAllowed(m,'poissonnier2026_ymaze'),/locked/);console.log('calibration-policy.test.js PASS');
